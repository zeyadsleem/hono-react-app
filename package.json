{
  "name": "hono-react-app",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun ./server/index.ts",
    "dev": "bun --watch index.ts",
    "build": "bun install && bun --prefix ./frontend install && bun --prefix ./fronten drun build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.1",
    "hono": "^4.3.0",
    "zod": "^3.23.6"
  }
}
